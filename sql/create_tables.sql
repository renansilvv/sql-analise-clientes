CREATE TABLE sales (
    order_date DATE,
    product VARCHAR(150),
    category VARCHAR(100),
    region VARCHAR(100),
    customer_name VARCHAR(150),
    quantity INT,
    sales DECIMAL(10,2),
    profit DECIMAL(10,2)
);
